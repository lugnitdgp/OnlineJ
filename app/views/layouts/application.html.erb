<!DOCTYPE html>
<html>
  <head>
    <%= render partial: 'layouts/head' %>
    <%= yield :styles %>
    <%= csrf_meta_tags %>
  </head>

  <body class="page-body page-fade-only" style="font-size: 15px;font-family: "Helvetica Neue", Helvetica, "Noto Sans", sans-serif;">
    <div class="page-container">
      <div class="main-content">
        <%= yield %>
      </div>
      <%= yield :scripts %>
    </div>
    <%= render 'layouts/scripts' %>
    </body>
</html>
